<% provide(:title, "29") %>

<%= render partial: 'partials/word_plot', locals: { proverb: @proverbs29 } %>

<h4>Search for a word to be highlighted, for statistical information and definition:</h4>
<%= form_tag(static_pages_proverbs29_path, method: :get) do %>
	<%= text_field_tag :proverb %>
	<%= submit_tag 'Search' %>
<% end %>
<br>

<div class="row">
	
	<%= render partial: 'partials/index_partial', locals: { proverb: @proverbs29 } %>

	<% if params[:commit] %>
		<div class="col-sm">
			<%= render partial: 'partials/html_mapping', locals: { chapter_num: @common, key_values: @key_values, client: @client, bwp: @bwp  } %><br><br>
		</div>
	<% end %>

</div>
