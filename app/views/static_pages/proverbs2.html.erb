% provide(:title, "2") %>

<h4>Search for a word to be highlighted:</h4>
<%= form_tag(static_pages_proverbs2_path, method: :get) do %>
	<%= text_field_tag :proverb %>
	<%= submit_tag 'Search' %>
<% end %>
<br><br>

<div class="row">

	<%= render partial: 'partials/index_partial', locals: { proverb: @proverbs2 } %>

  <div class="col-sm">
		<%= render partial: 'partials/html_mapping', locals: { chapter_num: @common } %><br><br>
	</div>

</div>
