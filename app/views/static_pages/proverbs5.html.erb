<% provide(:title, "5") %>

<h4>Search for a word to be highlighted:</h4>
<%= form_tag(static_pages_proverbs5_path, method: :get) do %>
	<%= text_field_tag :proverb %>
	<%= submit_tag 'Search' %>
<% end %>
<br><br>
<p>
	<% @proverbs5.each do |prov| %>
		<% if !@params.nil? %>
			<% if prov.include? @params %>
				<%= prov.sub("#{@params}", "<b>#{@params}</b>").html_safe %><br>
			<% else %> 
				<%= prov %> <br>
			<% end %> 
		<% else %>
			<%= prov %> <br>
		<% end %> 
	<% end %>
</p>

